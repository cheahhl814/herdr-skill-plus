{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/cheahhl814/herdr-skill-plus/quiz.schema.v1.json",
  "title": "Herdr-skill+ Quiz",
  "description": "Reference interchange schema for quiz / knowledge-check items rendered through herdr-skill+ §7 (ask_user_question gate). Importers (PDF chapters via docs-ingest+pymupdf4llm, literature paper Q&A, or hand-authored) emit this shape so §7 can render each item via the same per-item ask_user_question loop. §7 does not require this schema to be used; agent-authored inline items skip it. Schema v1 is intentionally minimal - one JSON document per quiz, one item per document line. Promoting schema-specific fields to required makes importers harder, not easier.",
  "type": "object",
  "required": ["schema", "title", "items"],
  "additionalProperties": true,
  "properties": {
    "schema": {
      "type": "string",
      "const": "herdr-skill+/quiz.v1",
      "description": "Schema URI / version. Bump the suffix when adding a breaking field. v1 = ask_user_question primitives only (2026-09-15)."
    },
    "title": {
      "type": "string",
      "description": "Short human-readable title for the quiz; surfaces as the first chat message after §7 setup."
    },
    "lesson_id": {
      "type": "string",
      "description": "Optional opaque id for cross-referencing with the §6 tutorial-mode lesson plan (e.g. 'ch3-sam-flags'). Not consumed by §7; supplied for replays / score-tracking."
    },
    "source": {
      "type": "object",
      "description": "Provenance of the items - which PDF, which chapter, which NotebookLM notebook. Optional; recommended for source-derived items, omitted for hand-authored ones.",
      "additionalProperties": true,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["pdf", "notebooklm", "manual", "html", "markdown", "other"]
        },
        "path": { "type": "string" },
        "pages": { "type": "array", "items": { "type": "integer", "minimum": 1 } },
        "chapter": { "type": "string" },
        "notebook_id": { "type": "string" },
        "extracted_by": {
          "type": "string",
          "description": "Tool chain that produced this quiz (e.g. 'docs-ingest+pymupdf4llm+llm-from-snippet'). For audit only."
        }
      }
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "description": "5-6 items recommended. §7 enforces one ask_user_question call per item; larger quizzes stress the per-turn context budget.",
      "items": { "$ref": "#/$defs/item" }
    }
  },
  "$defs": {
    "item": {
      "type": "object",
      "required": ["kind", "q"],
      "additionalProperties": true,
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["mcq", "multi", "preview", "short", "task"]
        },
        "q": {
          "type": "string",
          "description": "Question text rendered as ask_user_question.question (must end with '?' after author copy-edits)."
        },
        "options": {
          "type": "array",
          "minItems": 2,
          "maxItems": 4,
          "description": "Required for mcq / multi / preview; optional / forbidden for short and task.",
          "items": { "$ref": "#/$defs/option" }
        },
        "answer_key": {
          "type": "string",
          "description": "Reference answer for 'short' items. Free text the student types into the 'Type something.' row is graded against this. Accept obvious synonyms during grading (§7 anti-pattern: don't strict-match).",
          "minLength": 1
        },
        "task_id": {
          "type": "string",
          "description": "Opaque id linking a 'task' item to its §6 sibling lesson. Used by replay / score-tracking, not by §7 itself."
        },
        "instruction": {
          "type": "string",
          "description": "Plain-language instruction the agent issues in chat for a 'task' item (e.g. 'run samtools flagstat aln.bam')."
        },
        "gates": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "description": "For 'task' items: the two-option labels for the I-did-it / Something-went-wrong gate. Defaults to §6's canonical labels if absent.",
          "items": { "type": "string" }
        },
        "provenance": {
          "type": "object",
          "description": "Per-item source pointer (page, snippet, generated-by). Same structure as the top-level 'source.provenance' if needed.",
          "additionalProperties": true
        }
      }
    },
    "option": {
      "type": "object",
      "required": ["label"],
      "additionalProperties": true,
      "properties": {
        "label": {
          "type": "string",
          "minLength": 1,
          "maxLength": 60,
          "description": "ask_user_question option label; 1-5 words ideal."
        },
        "description": {
          "type": "string",
          "description": "ask_user_question option description (what choosing this means)."
        },
        "preview": {
          "type": "string",
          "description": "Markdown rendered side-by-side (≥100 cols). single-select (mcq/preview) only - ask_user_question forbids preview on multi-select."
        },
        "correct": {
          "type": "boolean",
          "description": "Grader flag. mcq: exactly one option must be correct. multi: zero or more; grade by set-equality. preview: exactly one. short/task: omit (use answer_key or task_id)."
        }
      }
    }
  }
}
